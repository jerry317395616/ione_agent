# MCP data contract

- Discover application DocTypes and fields before reading sensitive data.
- Use narrow filters, bounded limits and the minimum necessary fields.
- Only draft create/update and private text attachment are available.
- Settlement, submission, cancellation, deletion, SQL and arbitrary RPC are unavailable.
- Verify writes and separate observed evidence from model inference.
